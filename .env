# متغیرهای محیطی محلی (الگو: .env.example)
# ⚠️ در Railway از این فایل استفاده نمی‌شود — متغیرها را در Service ▸ Variables بگذارید.

# دیتابیس sqlite (اسکافلد Prisma؛ اپ چت از آن استفاده نمی‌کند)
DATABASE_URL="file:./db/custom.db"
